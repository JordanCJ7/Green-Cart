# Placeholder Dockerfile for Authentication service
FROM alpine:3.20
RUN addgroup -S app && adduser -S app -G app
WORKDIR /app
USER app
CMD ["sh", "-c", "echo Authentication service placeholder container"]
