name: Inventory CI

on:
  push:
    paths:
      - "inventory/**"
      - ".github/workflows/inventory-ci.yml"
      - ".github/workflows/_service-ci-template.yml"
  pull_request:
    paths:
      - "inventory/**"
      - ".github/workflows/inventory-ci.yml"
      - ".github/workflows/_service-ci-template.yml"

permissions:
  contents: read

jobs:
  service-ci:
    name: inventory - Service CI
    uses: ./.github/workflows/_service-ci-template.yml
    with:
      service_name: inventory
