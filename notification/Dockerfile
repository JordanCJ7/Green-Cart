# Placeholder Dockerfile for Notification service
FROM alpine:3.20
RUN addgroup -S app && adduser -S app -G app
WORKDIR /app
USER app
CMD ["sh", "-c", "echo Notification service placeholder container"]
