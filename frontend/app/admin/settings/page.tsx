import React from "react";

export default function AdminSettingsPage() {
  return (
    <div style={{ border: "1px solid #dce9de", borderRadius: "12px", background: "#fff", padding: "1rem" }}>
      <h1>Admin Settings</h1>
      <p>Settings center is now routed and ready for policy, notification, and pricing configuration modules.</p>
    </div>
  );
}
